spring:
  cloud:
    config:
      name: bootstrap.yml
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://206.189.197.225:5432/user_identity
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate: 
        jdbc:
          lob:
            non_contextual_creation: true